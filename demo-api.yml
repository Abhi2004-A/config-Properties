server:
  port: 8085

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111

management:
  endpoint:
    web:
      exposure:
        inclue: "*"

message: "This is Rest message from config server"
